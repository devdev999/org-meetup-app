# .claude/skills holds machine-local junctions into .agents/skills, created by the skills CLI.
# The canonical skill files live in .agents/skills and are committed; the links are recreated on install.
.claude/skills/

# Dependencies and build output
node_modules/
.next/
dist/
*.tsbuildinfo
test-results/
playwright-report/
next-env.d.ts

# Local environment
.env
.env.local
